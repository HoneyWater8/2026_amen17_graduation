{
  "rules": [
    {
      "id": "amen17-public-video-read",
      "allowed": {
        "origins": ["https://2026amen17graduation.vercel.app"],
        "methods": ["GET", "HEAD"],
        "headers": ["Range", "If-Range", "If-None-Match", "If-Modified-Since"]
      },
      "exposeHeaders": ["Accept-Ranges", "Content-Length", "Content-Range", "ETag", "Last-Modified"],
      "maxAgeSeconds": 86400
    }
  ]
}
