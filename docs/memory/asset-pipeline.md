---
name: asset-pipeline
description: 원본 자산은 커밋하지 않는다. 사진 재생성 절차와 로컬 영상 수령·연결 대기 상태
metadata:
  type: project
---

**원본 자산은 저장소에 넣지 않습니다.** 리사이즈본만 커밋하고, 원본은 로컬에만 둡니다.

| 자산 | 원본 | 저장소 |
|---|---|---|
| 여정 사진 | 첫 수령분 원본은 **2026-09-11 삭제** (Google Drive에 있을 것으로 추정) | `FE/public/journey/<slug>/{thumb,full}/NN.jpg` (12MB, 커밋됨) |
| 영상 | **2026-09-16 확인: `FE/public/video/`에 MP4 10개 존재**. 초원별 연결은 대기 중 | `FE/public/video/*`는 git 제외. 배포용 공개 URL 별도 필요 |

**Why:** 원본은 장당 수 MB입니다(하나로가족한마당 19장은 전부 7728×5152). 저장소에 넣으면 클론이 무거워지고, 어차피 화면에는 훨씬 작은 크기로만 쓰입니다. 재생성 스크립트가 있으면 원본을 커밋할 이유가 없습니다.

**How to apply:**

- 새 사진을 받으면 `scripts/resize-photos.py`의 `FOLDERS`에 `<한글 폴더명>: <영문 slug>`를 등록하고 실행합니다. `pip install pillow` 필요.
- 그다음 `FE/src/data/graduation.ts`에서 **장수만** 고칩니다 — 경로는 `photosOf(slug, count, label)`이 규칙으로 만듭니다. 파일명을 나열하지 마세요.
- **로컬에 사진 원본이 없습니다.** 사진을 다른 크기로 다시 뽑거나 크롭을 바꿔야 하면 발주 측(또는 Google Drive)에서 원본을 다시 받아야 합니다. 지금 있는 사진은 thumb 320w · full 1280w 두 벌뿐입니다.

## 로컬 영상 · 초원별 연결 대기

2026-09-16 확인한 파일은 다음과 같습니다. 이후 수령분이 추가될 수 있으므로 연결 작업 전에 실제 디렉터리를 다시 확인합니다.

- `장년 여자_어순종.mp4`
- `장년_감사영상_1.mp4`
- `장년_감사영상_3.mp4`
- `장년_감사영상_4.mp4`
- `장년_감사영상_5.mp4`
- `청년 1팀_OnlyHim.mp4`
- `청년 4팀_더드림가조.mp4`
- `청년 5팀_부어부어.mp4`
- `청년 감사영상_2.mp4`
- `청년_감사영상_5.mp4`

**초원명은 전달받았지만 파일과 초원의 대응은 아직 확인되지 않았습니다.** 파일명에 감사영상·팀 이름이 섞여 있으므로 개수가 10개라는 이유만으로 초원 순서에 맞춰 연결하지 않습니다. 원본 파일도 이동·이름 변경하지 않았습니다.

간증 섹션 참고 이미지 원본은 `C:\Users\kjyjy\Downloads\KakaoTalk_20260915_094358116.png`입니다. 사용자 제공 이미지이며 저장소에는 포함하지 않았습니다. 확정 초원명과 배치 순서는 `FE/src/data/graduation.ts`를 기준으로 확인합니다.

## 아이콘 · 썸네일

`FE/public/icons/`의 파일들도 원본에서 가공한 것이며 원본은 커밋하지 않습니다. 생성 스크립트를 따로 두지 않았으므로 방법만 적어둡니다 (Pillow).

| 파일 | 만든 방법 |
|---|---|
| `thumbnail.png` 1200×630 | 시안 스크린샷에서 배경색과 다른 픽셀의 bbox를 재어 콘텐츠만 크롭 → 상하 여백 14%를 두고 중앙 배치 → 남는 자리는 원본 배경색으로 채움 |
| `favicon-32/180.png` | `seal/wax-seal.png`의 알파 bbox를 크롭해 정사각 캔버스에 여백 8%로 중앙 배치 (투명 유지) |
| `apple-touch-icon.png` | 위와 같되 배경을 `EV.paper`로 채움 — iOS는 투명 영역을 검게 채운다 |

**남은 자산 작업** (2026-09-16 기준) — 제자 수업 사진 수령, 영상 용도·초원 대응 확인 및 배포용 URL 연결. 로컬 영상 수령과 화면 연결 완료를 구분합니다.
